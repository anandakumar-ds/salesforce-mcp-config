{
  "mcpServers": {
    "salesforce": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs", "ameyo",
        "--toolsets", "orgs,data,metadata,users",
        "--allow-non-ga-tools"
      ]
    }
  }
}
