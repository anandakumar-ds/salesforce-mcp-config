[mcp_servers.salesforce]
command = "npx"
args = ["-y", "@salesforce/mcp", "--orgs", "ameyo", "--toolsets", "orgs,data,metadata,users", "--allow-non-ga-tools"]
startup_timeout_sec = 30
tool_timeout_sec = 120
